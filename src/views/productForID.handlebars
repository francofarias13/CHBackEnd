<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles del Producto</title>
</head>
<body>
    <h1 class="ecommerce-title">Detalles del Producto</h1>

    {{#if product}}
    <div class="detail-product">
        <ul>
            <li><strong>ID:</strong> {{product._id}}</li>
            <li><strong>Título:</strong> {{product.title}}</li>
            <li><strong>Precio:</strong> {{product.price}} </li>
            <li><strong>Descripción:</strong> {{product.description}}</li>
            <li><strong>Stock:</strong> {{product.stock}}</li>
            <li><strong>Código:</strong> {{product.code}}</li>
            <li><strong>Estado:</strong> {{#if product.status}}Activo{{else}}Inactivo{{/if}}</li>
            <li><strong>Categoría:</strong> {{product.category}}</li>
            <li><strong>Creado en:</strong> {{product.createdAt}}</li>
            <li><strong>Actualizado en:</strong> {{product.updatedAt}}</li>
        </ul>
    </div>
       
    {{else}}
        <p>No se encontró el producto.</p>
    {{/if}}
</body>
</html>
